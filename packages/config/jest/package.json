{
  "name": "@frepiso/config-jest",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "jest": "^30.0.5"
  },
  "files": [
    "preset-api/jest-preset.js",
    "preset-node/jest-preset.js"
  ]
}
