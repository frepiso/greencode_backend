{
	"name": "@frepiso/config-esbuild",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
	},
	"devDependencies": {
		"esbuild": "^0.25.8",
		"esbuild-jest": "^0.5.0"
	}
}
